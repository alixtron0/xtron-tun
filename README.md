# XTRON-TUN 🚀

<div align="center">

![XTRON-TUN Logo](https://via.placeholder.com/800x200/0891b2/ffffff?text=XTRON-TUN)

**مدیریت حرفه‌ای تونل SMTP برای سرورهای داخل و خارج**

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Version](https://img.shields.io/badge/version-1.0.0-green.svg)](https://github.com/alixtron0/xtron-tun)
[![Platform](https://img.shields.io/badge/platform-Linux-lightgrey.svg)](https://github.com/alixtron0/xtron-tun)

[نصب](#-نصب) • [ویژگی‌ها](#-ویژگی‌ها) • [استفاده](#-استفاده) • [مستندات](#-مستندات) • [پشتیبانی](#-پشتیبانی)

</div>

---

## 📋 فهرست مطالب

- [درباره XTRON-TUN](#-درباره-xtron-tun)
- [ویژگی‌ها](#-ویژگی‌ها)
- [پیش‌نیازها](#-پیش‌نیازها)
- [نصب](#-نصب)
- [استفاده](#-استفاده)
  - [سرور خارج (Kharej)](#سرور-خارج-kharej)
  - [سرور ایران (Iran)](#سرور-ایران-iran)
- [معماری](#-معماری)
- [تنظیمات پیشرفته](#-تنظیمات-پیشرفته)
- [عیب‌یابی](#-عیب‌یابی)
- [مثال‌های کاربردی](#-مثال‌های-کاربردی)
- [سوالات متداول](#-سوالات-متداول)
- [مشارکت](#-مشارکت)
- [لایسنس](#-لایسنس)

---

## 🎯 درباره XTRON-TUN

**XTRON-TUN** یک ابزار قدرتمند و کاربرپسند برای مدیریت تونل‌های SMTP بین سرورهای داخل و خارج از کشور است. این ابزار با استفاده از تکنولوژی‌های مدرن مانند **GOST** و **socat**، امکان برقراری ارتباط امن و پایدار بین سرورها را فراهم می‌کند.

### چرا XTRON-TUN؟

- ✅ **نصب آسان با یک خط** - فقط یک دستور کپی و اجرا
- ✅ **رابط کاربری زیبا** - منوی تعاملی و کاربرپسند با رنگ‌بندی حرفه‌ای
- ✅ **پشتیبانی از SOCKS5** - با قابلیت احراز هویت
- ✅ **Port Forwarding انعطاف‌پذیر** - با دو موتور GOST و socat
- ✅ **مدیریت کامل** - شامل مانیتورینگ، لاگ‌گیری و مدیریت سرویس‌ها
- ✅ **امنیت بالا** - پشتیبانی از احراز هویت و رمزنگاری
- ✅ **خودکار** - راه‌اندازی سرویس‌های systemd برای پایداری بالا

---

## ✨ ویژگی‌ها

### سرور خارج (Kharej)

- 🌐 **راه‌اندازی SOCKS5 Proxy** با GOST v3
- 🔐 **احراز هویت کاربران** (اختیاری)
- 📦 **صادرات تنظیمات** به صورت فایل ZIP برای سرور ایران
- 📊 **مانیتورینگ** و نمایش وضعیت تونل‌ها
- 🧪 **Ping Test** برای بررسی اتصال
- 🗑️ **حذف و پاکسازی** کامل تونل‌ها

### سرور ایران (Iran)

- 📥 **وارد کردن تنظیمات** از فایل ZIP یا به صورت دستی
- ⚡ **Port Forwarding** با دو موتور:
  - **GOST** - قدرتمند و سریع
  - **socat** - سبک و ساده
- 🎯 **مدیریت چندین Port Forward** همزمان
- 📈 **نمایش وضعیت** تمام تونل‌ها و Port Forward ها
- 🔄 **شروع/توقف** سرویس‌ها به صورت تعاملی
- 🧹 **حذف کامل** تنظیمات با یک کلیک

### عمومی

- 📝 **لاگ‌گیری پیشرفته** با جزئیات کامل
- 🎨 **رابط کاربری زیبا** با استفاده از ANSI colors
- 🔧 **قابل تنظیم** برای نیازهای مختلف
- 🐧 **پشتیبانی از توزیع‌های مختلف** Linux (Ubuntu, Debian, CentOS, RHEL)

---

## 📦 پیش‌نیازها

### سیستم عامل

- Ubuntu 22.04+ / Debian 12+
- CentOS 9+ / RHEL 9+ / Fedora
- سایر توزیع‌های لینوکس با systemd

### دسترسی‌ها

- دسترسی `root` یا `sudo`
- اتصال به اینترنت برای دانلود بسته‌ها
- حداقل 512MB RAM
- حداقل 100MB فضای دیسک

### پورت‌ها

سرور خارج (Kharej):
- پورت SOCKS5 (پیش‌فرض: 1080) باید باز باشد

سرور ایران (Iran):
- پورت‌های Port Forward (مثلاً 2087, 2052) باید باز باشند

---

## 🚀 نصب

### ⚠️ نکته مهم: دسترسی Root الزامی است

**XTRON-TUN حتماً باید با دسترسی root اجرا شود.**

### نصب سریع (یک خطی)

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/alixtron0/xtron-tun/main/install.sh)
```

یا با `wget`:

```bash
sudo bash <(wget -qO- https://raw.githubusercontent.com/alixtron0/xtron-tun/main/install.sh)
```

### نصب دستی

```bash
# 1. دانلود اسکریپت نصب
curl -fsSL https://raw.githubusercontent.com/alixtron0/xtron-tun/main/install.sh -o install.sh

# 2. اجرای اسکریپت با sudo
sudo bash install.sh

# 3. پاک کردن فایل نصب
rm install.sh
```

### نصب از سورس

```bash
git clone https://github.com/alixtron0/xtron-tun.git
cd xtron-tun
sudo bash install.sh
```

---

## 📘 استفاده

### راه‌اندازی اولیه

بعد از نصب، **حتماً با دسترسی root** دستور زیر را اجرا کنید:

```bash
sudo xtron-tun
```

**نکته:** اگر بدون `sudo` اجرا کنید، خطای دسترسی دریافت خواهید کرد.

---

## سرور خارج (Kharej)

### گام 1: راه‌اندازی تونل جدید

1. از منوی اصلی گزینه **"1 • سرور خارج"** را انتخاب کنید
2. گزینه **"1 • راه‌اندازی تونل جدید"** را انتخاب کنید
3. اطلاعات زیر را وارد کنید:
   - نام تونل (مثال: `smtp-tunnel-1`)
   - آدرس سرور SMTP (مثال: `smtp.gmail.com`)
   - پورت‌های SMTP (مثال: `25`, `587`)
   - پورت SOCKS5 (پیش‌فرض: `1080`)
   - نام کاربری و رمز عبور (اختیاری)

### گام 2: صادرات تنظیمات

1. از منوی Kharej گزینه **"3 • صادرات تنظیمات"** را انتخاب کنید
2. تونل مورد نظر را انتخاب کنید
3. فایل ZIP در `/tmp/` ایجاد می‌شود
4. این فایل را به سرور ایران منتقل کنید

**مثال:**

```bash
# کپی فایل به سرور ایران با scp
scp /tmp/xtron-smtp-tunnel-1-config.zip user@iran-server:/tmp/
```

---

## سرور ایران (Iran)

### گام 1: راه‌اندازی تونل

1. از منوی اصلی گزینه **"2 • سرور ایران"** را انتخاب کنید
2. گزینه **"1 • راه‌اندازی تونل"** را انتخاب کنید
3. گزینه **"1 • استفاده از فایل ZIP"** را انتخاب کنید
4. مسیر فایل ZIP را وارد کنید (مثال: `/tmp/xtron-smtp-tunnel-1-config.zip`)
5. IP سرور خارج را وارد کنید

### گام 2: ایجاد Port Forward

1. از منوی Iran گزینه **"2 • Port Forward"** را انتخاب کنید
2. گزینه **"1 • ایجاد Port Forward جدید"** را انتخاب کنید
3. تونل را انتخاب کنید
4. موتور را انتخاب کنید:
   - **GOST** (پیشنهادی) - قدرتمند و سریع
   - **socat** - سبک و ساده
5. اطلاعات Port Forward را وارد کنید:
   - پورت محلی (مثال: `2087`)
   - پورت مقصد (مثال: `25`)
   - آدرس مقصد (پیش‌فرض: `127.0.0.1`)

---

## 🏗️ معماری

### ساختار کلی

```
┌─────────────────┐         ┌──────────────────┐         ┌─────────────────┐
│                 │         │                  │         │                 │
│  SMTP Client    │────────▶│  Iran Server     │────────▶│  Kharej Server  │
│  (Application)  │         │  (Port Forward)  │         │  (SOCKS5 Proxy) │
│                 │         │                  │         │                 │
└─────────────────┘         └──────────────────┘         └─────────────────┘
                                     │                            │
                                     │                            │
                                     ▼                            ▼
                            ┌─────────────────┐         ┌─────────────────┐
                            │                 │         │                 │
                            │  GOST / socat   │         │     GOST v3     │
                            │  (Forwarder)    │         │  (SOCKS5 Server)│
                            │                 │         │                 │
                            └─────────────────┘         └─────────────────┘
```

### جریان داده

1. **Client** درخواست را به **Iran Server** می‌فرستد (پورت محلی، مثلاً 2087)
2. **Port Forwarder** (GOST/socat) درخواست را از طریق **SOCKS5** به **Kharej Server** می‌فرستد
3. **Kharej Server** درخواست را به **SMTP Server** هدف می‌فرستد
4. پاسخ از مسیر معکوس برمی‌گردد

---

## ⚙️ تنظیمات پیشرفته

### پوشه‌های سیستم

```
/etc/xtron-tun/           # تنظیمات
├── kharej/               # تنظیمات سرور خارج
│   └── tunnel-name/      # تنظیمات هر تونل
└── iran/                 # تنظیمات سرور ایران
    └── tunnel-name/      # تنظیمات هر تونل
        └── port-forwards/ # Port Forward ها

/var/log/xtron-tun/       # لاگ‌ها
├── xtron.log             # لاگ اصلی
├── kharej.log            # لاگ سرور خارج
└── iran.log              # لاگ سرور ایران

/usr/local/bin/           # فایل‌های اجرایی
└── xtron-tun             # اسکریپت اصلی

/usr/local/lib/xtron-tun/ # کتابخانه‌ها
├── utils.sh              # توابع کمکی
├── kharej.sh             # ماژول سرور خارج
└── iran.sh               # ماژول سرور ایران
```

### مدیریت سرویس‌ها

**همه دستورات systemd نیاز به دسترسی root دارند:**

```bash
# نمایش وضعیت تونل (Kharej)
sudo systemctl status xtron-tunnel-name.service

# نمایش وضعیت Port Forward (Iran)
sudo systemctl status xtron-pf-pf-name.service

# لاگ‌های زنده
sudo journalctl -u xtron-tunnel-name.service -f

# ریستارت سرویس
sudo systemctl restart xtron-tunnel-name.service
```

---

## 🔧 عیب‌یابی

### مشکل: تونل متصل نمی‌شود

**راه‌حل:**

1. بررسی کنید سرویس در حال اجرا باشد:
   ```bash
   sudo systemctl status xtron-tunnel-name.service
   ```

2. بررسی لاگ‌ها:
   ```bash
   sudo tail -f /var/log/xtron-tun/tunnel-name.log
   ```

3. بررسی فایروال:
   ```bash
   # Ubuntu/Debian
   sudo ufw status

   # CentOS/RHEL
   sudo firewall-cmd --list-all
   ```

### مشکل: Port Forward کار نمی‌کند

**راه‌حل:**

1. تست اتصال SOCKS5:
   ```bash
   curl --socks5 kharej-ip:1080 https://ifconfig.me
   ```

2. بررسی پورت محلی:
   ```bash
   ss -tuln | grep 2087
   ```

3. تست Port Forward:
   ```bash
   nc -zv localhost 2087
   ```

### مشکل: خطای احراز هویت SOCKS5

**راه‌حل:**

1. بررسی نام کاربری و رمز عبور در فایل تنظیمات
2. مطمئن شوید که GOST با احراز هویت راه‌اندازی شده
3. لاگ‌های GOST را بررسی کنید

---

## 💡 مثال‌های کاربردی

### مثال 1: تونل SMTP Gmail

**سرور خارج:**

```
نام تونل: gmail-tunnel
سرور SMTP: smtp.gmail.com
پورت SMTP: 587
پورت SOCKS5: 1080
احراز هویت: myuser / mypassword
```

**سرور ایران:**

```
تونل: gmail-tunnel
موتور: GOST
پورت محلی: 2087
پورت مقصد: 587
آدرس مقصد: smtp.gmail.com
```

### مثال 2: چند Port Forward همزمان

```bash
# Port Forward 1: SMTP
پورت محلی: 2087 → smtp.server.com:25

# Port Forward 2: SMTPS
پورت محلی: 2052 → smtp.server.com:587

# Port Forward 3: IMAP
پورت محلی: 2143 → imap.server.com:143
```

---

## ❓ سوالات متداول

### آیا می‌توانم از این ابزار برای سرویس‌های غیر SMTP استفاده کنم؟

بله! XTRON-TUN می‌تواند برای هر نوع TCP port forwarding استفاده شود، نه فقط SMTP.

### تفاوت GOST و socat چیست؟

- **GOST**: قدرتمند‌تر، سریع‌تر، و با قابلیت‌های بیشتر (پیشنهادی)
- **socat**: سبک‌تر و ساده‌تر برای نیازهای پایه

### آیا می‌توانم چند تونل همزمان داشته باشم؟

بله! می‌توانید تعداد نامحدود تونل و Port Forward ایجاد کنید.

### آیا این ابزار امن است؟

بله! XTRON-TUN از SOCKS5 با احراز هویت استفاده می‌کند و تمام ارتباطات را از طریق تونل امن می‌کند.

---

## 🤝 مشارکت

مشارکت شما خوش آمد است! برای مشارکت:

1. این مخزن را Fork کنید
2. یک Branch جدید بسازید (`git checkout -b feature/AmazingFeature`)
3. تغییرات خود را Commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. Branch را Push کنید (`git push origin feature/AmazingFeature`)
5. یک Pull Request باز کنید

---

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است. برای جزئیات بیشتر [LICENSE](LICENSE) را مطالعه کنید.

---

## 📞 پشتیبانی

- **GitHub Issues**: [alixtron0/xtron-tun/issues](https://github.com/alixtron0/xtron-tun/issues)
- **Email**: support@xtron.ir
- **Telegram**: [@xtron_support](https://t.me/xtron_support)

---

<div align="center">

**ساخته شده با ❤️ توسط [AliXtron](https://github.com/alixtron0)**

اگر این پروژه برای شما مفید بود، یک ⭐ بدهید!

</div>
